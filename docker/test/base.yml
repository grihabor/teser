version: "2"
services:
  gunicorn:
    image: grihabor/testing-server:gunicorn
    build: 
      context: ../..
      dockerfile: docker/test/Dockerfile
    environment:
      - PYTHONPATH=/project/src
    env_file:
      - ./config.env
